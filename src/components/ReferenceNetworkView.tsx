import { ReferenceFieldsDisplay } from './ReferenceFieldsDisplay';

export function ReferenceNetworkView() {
  // For Story 2.2, we're using a basic data display instead of complex D3 visualization
  // This can be upgraded to a force-directed graph later
  return <ReferenceFieldsDisplay />;
}